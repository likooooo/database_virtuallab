# geometries

Lossy VL interfaces/boundaries → core shapes. Refresh: `../update_current_database.py`.
Skips: `_export_log/geo_skip.txt` after export (also mirrored under tools notes when refreshed).
See [../README.md](../README.md).
